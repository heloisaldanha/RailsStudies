<!-- Todo arquivo partial começa com um underscore -->

<nav class="navbar navbar-inverse navbar-static-top">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#expanded-menu" aria-expanded=" false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      
      </button>
      <% if !user_signed_in? %>
        <%= link_to 'Agendinha', root_url, class: 'navbar-brand' %>
      <% else %>
        <%= link_to 'Agendinha', contacts_path, class: 'navbar-brand' %>
      <% end %>
    </div>

  <!-- Conteudo exibido em telas maiores e que serao escondidos em telas menores -->
    <div class="collapse navbar-collapse" id="expanded-menu">
        <ul class="nav navbar-nav">
        <% if !user_signed_in? %>
            <li class="active">
                <%= link_to 'Nova conta', new_user_path %>
            </li>
        <% end %>
        </ul>

        <ul class="nav navbar-nav navbar-right">

            <% if !user_signed_in? %>
              <li>
                <%= link_to 'Entrar', entrar_path %>  <!-- entrar_path pois foi criado uma rota chamada entrar no routes -->
              </li>
            <% else %>

            
            <%= link_to 'Editar', edit_user_path(current_user), class: 'navbar-brand' %>
            <%= link_to 'Sair', sair_path,  method: :delete, class: 'navbar-brand' %>
            

            <% end %>


        </ul>

    </div>

  </div>


</nav>